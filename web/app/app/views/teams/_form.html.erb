<%= form_for(@team,:html=>{role: "form"}) do |f| %>
	<% if @team.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@team.errors.count, "error") %> prohibited this functionality from being saved:</h2>

			<ul>
				<% @team.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="form-group row">
		<div class='col-md-6'>
			<%= f.label :name,class: 'label-class' %><br>
			<%= f.text_field :name,class: 'form-control',required: 'required',autofocus: true %>
		</div>
	</div>
	<div class="form-group row">
		<div class='col-md-6'>
			<%#= f.label :roles,class: 'label-class' %>
			<%#= f.select :role_ids, Role.all.order(:name).collect { |t| [t.name,t.id]},{prompt: 'Select Role'},{:class=> "selectpicker dropup show-tick show-menu-arrow form-control",'data-live-search'=>"true",'data-show-subtext'=> "true",'data-size'=>"5",'multiple'=>'multiple'}%>
		</div>
	</div>

	<div class="form-group row">
		<div class='col-md-6'>
			<%= f.submit "Save" ,class: 'btn btn-success'%>
		</div>
	</div>
<% end %>
