<%=custom_bootstrap_flash%>
$(".spinner_section").spin(false);
var table = $('#<%=params[:marked][:table_id]%>').DataTable();
table.draw( 'page' );
'<%if params[:marked][:table_id]!="filter_findings_table"%>'
	'<%@not_false_sev_hash.each do |k,v|%>'
		$("#not_false_" + '<%=k.downcase%>').text('<%=v%>');
	'<%end%>'
	'<%if current_user.is_admin? || current_user.is_security_member?%>'
		'<%@false_sev_hash.each do |k,v|%>'
			$("#false_" + '<%=k.downcase%>').text('<%=v%>');
		'<%end%>'
	'<%end%>'
'<%end%>'
//table.fnDraw();
